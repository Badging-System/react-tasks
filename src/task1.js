import React from "react"; //Importing into App?
export default function task1(props) { //This should export the task1 function to App.js
    return (
        <div>
            <h2>This should say Task1</h2> 
        </div>
    ); //Returns text as the header
}